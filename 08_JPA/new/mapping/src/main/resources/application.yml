spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/menudb
    username: ohgiraffers
    password: ohgiraffers

  jpa:
    show-sql: true
    database: mysql
    hibernate:
      ddl-auto: create # when start program executing drop table and create table
    properties:
      hibernate:
        format_sql: true

